# C# course

## Compile & run

```sh
# Use your preferred compiler to compile C++ code, then run the program
g++ ./LESSON/FILE.cpp -std=c++11 -o LESSON/FILE.out && ./LESSON/FILE.out

# Example
g++ ./lesson-1/is-even.cpp -std=c++11 -o lesson-1/is-even.out && ./lesson-1/is-even.out
```
